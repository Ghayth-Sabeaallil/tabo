
const Dashboard: React.FC = () => {
    return (
        <main className="flex flex-col items-center justify-center h-screen">
            <div>
                <h1>Welcome, GHAYTH!</h1>
                <button>Logout</button>
            </div>
        </main>
    );
};

export default Dashboard;